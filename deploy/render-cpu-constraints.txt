# Render web-service constraints: force the CPU-only PyTorch wheels so the
# community analyzer does not install CUDA runtime packages on CPU hosts.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.14.0+cpu
torchvision==0.29.0+cpu
